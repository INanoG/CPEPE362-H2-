import 'package:flutter/material.dart';

class RegisterViewDesktop extends StatelessWidget {
  const RegisterViewDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color.fromARGB(255, 255, 174, 160),
    );
  }
}
